package com.nyavro.tobuy.services.routes

import akka.http.scaladsl.server.Route
import com.nyavro.tobuy.services.RouteProvider

class GroupRoute {

  def route = {
//    pathPrefix("group") {
//      path("signUp") {
//        pathEndOrSingleSlash {
//          post {
//            entity(as[User]) { user =>
//              complete(Created -> authService.signUp(user).map(toUserToken).map(_.toJson))
//            }
//          }
//        }
//      } ~
//        path("signIn") {
//          pathEndOrSingleSlash {
//            post {
//              entity(as[LoginRequest]) { loginRequest =>
//                complete(authService.signIn(loginRequest.login, loginRequest.password).map(_.toJson))
//              }
//            }
//          }
//        }
//    }
  }
}
