import {EActionTypes} from 'Libraries/Core/Utils/ReduxUtils';

const CLIENT = 'CLIENT_';

export const LOAD_CLIENT = CLIENT + EActionTypes.LOAD_ITEM;
export const UPDATE_CLIENT = CLIENT + EActionTypes.UPDATE_ITEM;
